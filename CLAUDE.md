Serwer REST API dla projektów vue.

- odpowiadaj zawsze po polsku
- aby dostać się do danych na zdalnym serwerze 's1.ic26warsaw.pl' trzeba użyć ssh: login 'marek', hasło na bazie klucza prywatnego w lokalizacja dla klienta ssh
- w pliku mongo.go dane adresowe do lokalnej i zdalne bazy danych (po wcześniejszym zalogowaniu)